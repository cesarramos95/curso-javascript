function adicionar() {
    let numAd = window.document.getElementById('num')

    if(numAd.value.length == 0) {
        window.alert('Por favor, digite um número para começar!')
    } else {
        let n = Number(numAd) // Faz o casting da variável para tipo numérico   
        n.push()
    }
}